# 📊 每日报告归档系统使用指南

## 功能说明

自动归档每日生成的所有股票分析报告，按日期组织，方便历史回顾和对比分析。

## 目录结构

```
reports/
├── INDEX.md                    # 所有报告的索引文件
└── YYYY-MM-DD/                 # 按日期归档
    ├── DAILY_SUMMARY.md        # 每日综合摘要
    ├── market_env_*.txt        # 大盘环境分析
    ├── strategy_report_*.md    # 策略A扫描报告
    ├── oversold_report_*.md    # 策略B扫描报告
    ├── s_stocks_tracking_*.md  # S级股票跟踪
    ├── daily_comprehensive_*.md # 每日综合报告
    ├── csv/                    # CSV数据文件
    │   ├── six_dimension_scan_*.csv
    │   └── oversold_bounce_scan_*.csv
    └── logs/                   # 日志文件
        ├── strategy_scan.log
        ├── oversold_scan.log
        └── s_stocks_tracking.log
```

## 使用方法

### 1. 归档今日报告

```bash
# 自动归档今天生成的所有报告
python3 scripts/archive_daily_reports.py
```

### 2. 归档指定日期

```bash
# 归档指定日期的报告
python3 scripts/archive_daily_reports.py --date 2026-02-03
```

### 3. 查看报告索引

打开 `reports/INDEX.md` 查看所有日期的报告索引表格。

### 4. 查看每日摘要

打开 `reports/YYYY-MM-DD/DAILY_SUMMARY.md` 查看当日综合摘要。

## 报告内容

### 每日综合摘要 (DAILY_SUMMARY.md)

包含：
- 📁 **报告文件索引**：所有报告和数据文件的链接
- 🌍 **市场环境概览**：大盘评分、技术面分析
- 📊 **扫描结果汇总**：策略A和策略B的统计数据
- 💡 **策略建议**：根据市场环境的操作建议
- 🛡️ **风控提示**：止损和仓位管理建议

### 原始报告

- **大盘环境分析** (`market_env_*.txt`): 市场环境评分和技术面分析
- **策略A报告** (`strategy_report_*.md`): 六维真强势扫描结果，包含50只S级股票详情
- **策略B报告** (`oversold_report_*.md`): 黄金坑反弹扫描结果，包含超跌股分析
- **S级股票跟踪** (`s_stocks_tracking_*.md`): 跟踪昨日筛选股票的今日表现
- **每日综合报告** (`daily_comprehensive_*.md`): 整合大盘和个股的完整分析

### CSV数据 (csv/ 目录)

- **策略A数据** (`six_dimension_scan_*.csv`): 所有A级以上股票的详细数据
- **策略B数据** (`oversold_bounce_scan_*.csv`): 所有超跌股的详细数据

可用Excel或Python pandas分析这些数据。

## 自动化建议

### 每日定时归档

在 crontab 中添加定时任务：

```bash
# 每天收盘后 16:00 自动归档
0 16 * * 1-5 cd /path/to/daily_stock_analysis && python3 scripts/archive_daily_reports.py
```

### 集成到现有脚本

在生成报告后自动归档：

```python
from scripts.archive_daily_reports import DailyReportArchiver

# 生成报告后
archiver = DailyReportArchiver()
archiver.archive()
```

## 历史对比分析

### 1. 对比不同日期的S级股票

```bash
# 查看不同日期的CSV文件
diff reports/2026-02-03/csv/six_dimension_scan_*.csv \
     reports/2026-02-04/csv/six_dimension_scan_*.csv
```

### 2. 统计历史趋势

使用Python分析历史数据：

```python
import pandas as pd
from pathlib import Path

# 读取多日数据
dates = ['2026-02-03', '2026-02-04']
dfs = []
for date in dates:
    csv_file = f'reports/{date}/csv/six_dimension_scan_{date}.csv'
    if Path(csv_file).exists():
        df = pd.read_csv(csv_file)
        df['date'] = date
        dfs.append(df)

# 合并分析
all_data = pd.concat(dfs)
# ... 你的分析代码
```

### 3. 追踪股票表现

查看某只股票在不同日期的表现：

```python
stock_code = '600123'
for date in dates:
    csv_file = f'reports/{date}/csv/six_dimension_scan_{date}.csv'
    df = pd.read_csv(csv_file)
    stock = df[df['code'] == stock_code]
    if not stock.empty:
        print(f"{date}: {stock.iloc[0]['name']} - {stock.iloc[0]['six_dim_score']}分")
```

## 注意事项

1. **存储空间**: 每日报告约占用 5-10MB，注意定期清理旧报告
2. **备份**: 建议定期备份 `reports/` 目录到云存储
3. **索引文件**: `INDEX.md` 会自动更新，无需手动编辑

## 示例：2026-02-04 归档内容

### 本次归档包含：

- ✅ 5个Markdown报告
- ✅ 2个CSV数据文件  
- ✅ 3个日志文件
- ✅ 1个综合摘要
- ✅ 更新索引文件

### 关键数据：

- **市场环境**: 🟢 10/10分 - 绿灯
- **策略A**: 50只S级股票，182只A级以上
- **策略B**: 22只中度超跌股
- **Top股票**: 山西蓝焰科技(600123) - 10/10分

---

**最后更新**: 2026-02-04 22:32
